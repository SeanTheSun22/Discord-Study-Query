package discordstudyquery.guild.structure;

public class Thread extends AbstractDiscordContainer {
    public Thread(String name, String id, Channel parent) {
        super(name, id, parent);
    }
}
